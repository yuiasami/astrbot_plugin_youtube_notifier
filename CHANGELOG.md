# Changelog

本文件记录 `astrbot_plugin_youtube_notifier` 的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [v1.1.0] - 2026-09-18

### 新增

- 新增 `notify.style` 配置项：`image`（默认）继续使用 PIL 图片通知，`text`
  改为纯文字通知，并跳过封面下载与图片渲染；测试通知会带 `🧪 [测试]` 标记。

### 修复

- 修复频道订阅时未在直播、之后第一次真正开播会被错误静默吞掉的问题。新增独立的
  `live_seeded` 播种状态，不再把可能长期保持为 `none` 的 `last_status` 当作首次接入标志。
  多会话重复订阅同一频道会重跑播种的既有限制仍保留，本条不宣称覆盖该场景。

## [v1.0.1] - 2026-09-12

### 新增

- **`/yt批量订阅 <目标1> <目标2> ...`** —— 一次订阅多个频道，目标之间用空格分隔，
  可混用 `@handle` / 频道ID / 频道URL。
- **`/yt批量取消订阅 <目标1> <目标2> ...`** —— 一次取消多个订阅，目标之间用空格
  分隔，额外接受**频道名**（如 `NASA`）。

批量命令单条消息最多处理 20 个目标（每个新频道要花 1 单位 `channels.list`
+ 2 单位快照配额，且几十个网络往返会把消息处理卡到超时）；超出的部分会在回复里
**逐个列出**说明未处理，不会静默丢弃。

### 变更

- `/yt订阅` 与 `/yt批量订阅`、`/yt取消订阅` 与 `/yt批量取消订阅` 改为共用同一份
  实现（`_subscribe_one` / `_unsubscribe_one`），单条命令的回复文案与行为保持不变。
- 降级 / 「监控尚未生效」提示在批量回复里只出现**一次**（判据与 `/yt订阅` 完全一致），
  不按频道重复刷屏。

### 修复

- **`/yt取消订阅` 不再消耗 Data API 配额**：改为先在本地已订阅列表里匹配
  （频道ID → @handle → 频道名），匹配不到才查一次 API。此前只比对频道 ID，
  因此 `/yt取消订阅 @NASA` 这类 handle 输入每次都要花 1 单位配额。
- **`/yt取消订阅 @handle` 在 API 不可用（配额耗尽 / Key 无效 / 网络不通）时
  会误报「本会话未订阅该频道」**，现在本地即可匹配，不受数据源状态影响。
- 输入本身就是频道 ID 且本会话已订阅时，`/yt订阅` 直接回「已订阅」，
  不再为此多查一次 API（批量重发同一份 ID 列表时按频道个数省配额）。

## [v1.0.0] - 2026-09-12

首个版本。

### 新增

- 订阅 YouTube 频道（支持 `@handle` / 频道ID / 频道URL），直播上播、下播与新投稿
  以 **PIL 渲染的图片通知**推送；订阅按会话（`event.unified_msg_origin`）隔离。
- 直播状态机：上播 / 下播判定、时长计算（优先 `actualEndTime`）、同一 `live_id`
  只推一次，并防「直播结束后存档被当成新投稿」重复推送。
- 数据源与降级链：**YouTube Data API v3（API Key，主数据源）** → 网页 JSON
  （`ytInitialData`，无 Key 时也能监控）→ legacy Atom feed（最后兜底）。
  降级全程留痕：日志 + `/yt列表` 展示 + 订阅回复提示。
- 指令：`/yt订阅` `/yt取消订阅` `/yt列表` `/yt直播测试` `/yt视频测试`。
- 通知图与封面定时清理（按年龄 7 天 + 按总量 500MB，默认每天 4:00）。
- WebSub 实时推送（可选，默认关闭；端点不可靠，主要靠轮询）。
- 数据源诊断脚本 `scripts/diagnose.py`、OAuth 授权脚本 `scripts/oauth_setup.py`。

### 修复（首版发布前）

- Linux VPS 最小化安装下中文全部渲染成方框：候选字体链里混入了纯拉丁字体的
  `DejaVuSans.ttf` 并被静默命中。现在只把含中文字形的字体放进候选链，
  纯拉丁字体仅在确实找不到中文字体时兜底（同时打 ERROR + 安装指引）。
- Docker 部署下 `render.font_path` 指向宿主机路径无效：报错时识别容器环境并给出
  三条修复路径，同时提供 `<data_dir>/fonts/`（容器内可见且重启不丢）放字体。
- 适配器超时（NapCat/QQ NT 的 `sendMsg` 抛 `ActionFailed(retcode=1200)`）被误报为
  推送失败，而图片其实已经送达。现在区分「超时（可能已送达）」与「确认失败」：
  超时不重试、如实说明、且每条通知只告警一次。
